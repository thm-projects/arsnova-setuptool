{
	"_id": "_design/learning_progress_user_value",
	"language": "javascript",
	"views": {
		"user_value": {
			"map": "function(doc) { if (doc.type === 'skill_question_answer') { emit([doc.sessionId, doc.user], Math.min(doc.questionValue || 0, 0)); } }",
			"reduce": "_sum"
		}
	}
}
