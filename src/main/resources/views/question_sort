{
	"_id": "_design/question_sort",
	"language": "javascript",
	"views": {
		"sortOrder_by_session": {
			"map": "function(doc) { if (doc.type == 'question_sort') { emit ( doc.sessionID, doc.sortOrder ); } }"
		},
		"lecture_question_by_session_sorted": {
			"map": "function(doc) { if (doc.type == 'question_sort') { emit( doc.sessionId, null ) { for (var i in doc.sortOrder) { if (i.type == 'skill_question' && i.questionVariant == 'lecture') { emit([i.sessionId, i.subject, i.text], i); } } } } }"
		},
		"lecture_question_by_session_sorted_for_all": {
			"map": "function(doc) { if (doc.type == 'question_sort') { emit( doc.sessionId, null ) { for (var i in doc.sortOrder) { if (i.type == 'skill_question' && i.questionVariant == 'lecture' && i.active == 1) { emit([i.sessionId, i.subject, i.text], i); } } } } }"
		},
		"preparation_question_by_session_sorted": {
			"map": "function(doc) { if (doc.type == 'question_sort') { emit( doc.sessionId, null ) { for (var i in doc.sortOrder) { if (i.type == 'skill_question' && i.questionVariant == 'preparation') { emit([i.sessionId, i.subject, i.text], i); } } } } }"
		},
		"preparation_question_by_session_sorted_for_all": {
			"map": "function(doc) { if (doc.type == 'question_sort') { emit( doc.sessionId, null ) { for (var i in doc.sortOrder) { if (i.type == 'skill_question' && i.questionVariant == 'preparation' && i.active == 1) { emit([i.sessionId, i.subject, i.text], i); } } } } }"
		}
	}
}
